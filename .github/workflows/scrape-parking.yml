name: Scrape truck parking availability

on:
  schedule:
    # Every 2 hours, so short (hourly/daily) bookings are less likely to
    # come and go unseen between snapshots. Edit this cron to change frequency.
    - cron: "0 */2 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: scrape-parking
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run scraper
        run: python3 scraper/scrape.py

      - name: Commit results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # urls.txt too: the market sweep auto-appends newly discovered listings
          git add scraper/data scraper/urls.txt
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "Scrape parking data $(date -u +'%Y-%m-%dT%H:%M:%SZ')"
          # Retry push in case another run landed a commit meanwhile
          for i in 1 2 3; do
            git push && exit 0
            git pull --rebase
          done
          git push
